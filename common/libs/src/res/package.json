{
  "name": "res",
  "version": "1.0.0",
  "description": "",
  "main": "res.js",
  "scripts": {
    "dev": "nodemon -e js -x \"npm run build\"",
    "dev-poll": "nodemon -e js -x \"npm run build\" -L",
    "prebuild": "npm run test",
    "build": "npm pack --pack-destination ../../packs/res/",
    "postbuild": "cd ../../../app/ && npm i res --force --nofund --no-audit",
    "test": "echo \"Error: no test specified\""
  },
  "author": "lcluber",
  "license": "ISC",
  "files": ["res.js"],
  "dependencies": {
    "@dwtechs/checkard": "2.28.0",
    "@dwtechs/sparray": "0.1.1",
    "@dwtechs/winstan": "0.4.0"
  },
  "devDependencies": {
    "nodemon": "2.0.22"
  }
}
