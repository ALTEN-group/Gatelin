DOCKER_DEFAULT_PLATFORM="linux/amd64"
ENV_NAME=prod
APP_NAME=ookonva
APP_WEBSITE=https://ookonva.com
STACK_NAME=${APP_NAME}-${ENV_NAME}
APP_VERSION=latest
REPO_URL=vps-36553f06.vps.ovh.net
REPO_PORT=5555
SERVER_SCHEME=http://
SERVER_URL=localhost
SYSTEM_EMAIL=noreply@ookonva.com
DEFAULT_PORT=3000
FRONT_PORT=80
TZ=Europe/Paris
LOCALE=fr-FR

UID=1000
GID=1000

NODE_VERSION=22.9.0-alpine3.20
NODE_ENV=production
HOME_PATH=/home/user/

APT_REPOSITORY=http://deb.debian.org/debian
APK_REPOSITORY=https://dl-cdn.alpinelinux.org/alpine/v3.20/main
NPMRC="package-lock=false
strict-ssl=false"

# Liquibase 
LIQUIBASE_VERSION=4.24.0-alpine

# Adminer
ADMINER_HOST=${APP_NAME}-adminer-${ENV_NAME}
ADMINER_VERSION=adminer:latest
ADMINER_PORT=8080

# Nginx
NGINX_HOST=${APP_NAME}-nginx-${ENV_NAME}
NGINX_VERSION=1.25.3-alpine3.18-slim

#TRAEFIK
TRAEFIK_HOST=${APP_NAME}-traefik-${ENV_NAME}
TRAEFIK_VERSION=2.10.7
TRAEFIK_ACCESSLOG=true
TRAEFIK_API_INSECURE=true
TRAEFIK_ENTRYPOINTS_WEB_ADDRESS=":80"
TRAEFIK_ENTRYPOINTS_WEB_FORWARDEDHEADERS_INSECURE=false
TRAEFIK_PROVIDERS_DOCKER_EXPOSEDBYDEFAULT=false
TRAEFIK_LOG_LEVEL="DEBUG"
TRAEFIK_PING=true
TRAEFIK_DASH_PORT=8083


#rabbitmq
AMQP_HOST=${APP_NAME}-rabbitmq-${ENV_NAME}
AMQP_VERSION=3.13.0-management-alpine
AMQP_DEFAULT_USER=fjsdlmzdd
AMQP_DEFAULT_PASSWORD=ntroipve?F00F!!
AMQP_DEFAULT_VHOST=/
AMQP_HEARTBEAT=60
AMQP_LISTENERS_TCP_DEFAULT=5672
AMQP_MANAGEMENT_TCP_PORT=15672
AMQP_PROTOCOL=amqp
AMQP_RECONNECTION_LIMIT=3

# Postgres
POSTGRES_HOST=${APP_NAME}-postgres-${ENV_NAME}
POSTGRES_VERSION=postgres:16.2-alpine3.19
POSTGRES_PORT=5432
POSTGRES_ROOT_USER=root
POSTGRES_ROOT_PWD=fhsdjdkzelfbjklsd78!!?
POSTGRES_DBS=

# Postgis
POSTGIS_HOST=${APP_NAME}-postgis-${ENV_NAME}
POSTGIS_VERSION=postgis/postgis:16-3.4-alpine
POSTGIS_ROOT_USER=root
POSTGIS_ROOT_PWD=fnsdjfkpzeqfhcd??6786!
POSTGIS_DBS=msgateway,msuser,msnotif,msmail,mscron,msevent

# Kong
KONG_MIGRATION_HOST=${APP_NAME}-kong-migration-${ENV_NAME}
KONG_HOST=${APP_NAME}-kong-${ENV_NAME}
KONG_VERSION=3.1.1-alpine
KONG_DB_USER=thumk
KONG_DB_PWD=azdrft?fhsdjkfl76!!
KONG_PROXY_PORT=8000
KONG_PROXY_PORT_SSL=8443
KONG_PROXY_LISTEN=0.0.0.0:${KONG_PROXY_PORT}
KONG_PROXY_LISTEN_SSL=0.0.0.0:${KONG_PROXY_PORT_SSL}
KONG_ADMIN_PORT=8001
KONG_ADMIN_LISTEN=0.0.0.0:${KONG_ADMIN_PORT}
KONG_ADMIN_URL=${SERVER_SCHEME}${KONG_HOST}:${KONG_ADMIN_PORT}

# mock
MOCK_HOST=${APP_NAME}-mock-${ENV_NAME}
MOCK_NODE_VERSION=${NODE_VERSION}

# admin
ADMIN_HOST=${APP_NAME}-admin-${ENV_NAME}
ADMIN_PORT=80
ADMIN_NODE_VERSION=${NODE_VERSION}
ADMIN_URL=${SERVER_SCHEME}${ADMIN_HOST}:${ADMIN_PORT}/admin/

# mobile
MOBILE_HOST=${APP_NAME}-mobile-${ENV_NAME}
MOBILE_PORT=80
MOBILE_NODE_VERSION=${NODE_VERSION}
MOBILE_URL=${SERVER_SCHEME}${MOBILE_HOST}:${MOBILE_PORT}/mobile/

# web
WEB_HOST=${APP_NAME}-web-${ENV_NAME}
WEB_PORT=80
WEB_NODE_VERSION=${NODE_VERSION}
WEB_URL=${SERVER_SCHEME}${WEB_HOST}:${WEB_PORT}/web/

# ms_user
MSUSER_MIGRATION_HOST=${APP_NAME}-liquibase-ms-user-${ENV_NAME}
MSUSER_HOST=${APP_NAME}-ms-user-${ENV_NAME}
MSUSER_NODE_VERSION=${NODE_VERSION}
MSUSER_FOLDER=user
MSUSER_DB_NAME=msuser
MSUSER_DB_USER=mhlju
MSUSER_DB_PWD=gkdfrt§jdsqkldqsdhqsdls78!??
MSUSER_DB_MAX=50
MSUSER_DB_VERSION=1
MSUSER_PWD_SECRET=c0kjXXeM//sptAK6C6GEyEnmH5eVwAq09a+SEBZe/biDkdxL7jHIdqL+zzWPexNUMzjIyrHekuPwfh8lK6Hq4Q==
MSUSER_PWD_MIN_LENGTH=8
MSUSER_PWD_MAX_LENGTH=128
MSUSER_PWD_LENGTH=12
MSUSER_PWD_NUMBERS=true
MSUSER_PWD_UPPERCASE=true
MSUSER_PWD_LOWERCASE=true
MSUSER_PWD_SYMBOLS=false
MSUSER_PWD_STRICT=true
MSUSER_PWD_SIMILAR_CHARS=false
MSUSER_FIRSTNAME_MIN_LENGTH=2
MSUSER_LASTNAME_MIN_LENGTH=2
MSUSER_FIRSTNAME_MAX_LENGTH=50
MSUSER_LASTNAME_MAX_LENGTH=50
MSUSER_TOKEN_SECRET=604092140a2908a10c3a7fe3abb4b569497d00d30ef4fef2d5934a93c80a50ff7d3b529b97f2f74dbe8e9928cd88e1b182c4122385d5a6303a90ba5e7b29f23c
MSUSER_ACCESS_TOKEN_DURATION=600 #10 * 60 => 10 mins
MSUSER_REFRESH_TOKEN_DURATION=86400 #24 * 60 * 60 => 1 day

# ms_notif
MSNOTIF_MIGRATION_HOST=${APP_NAME}-liquibase-ms-notif-${ENV_NAME}
MSNOTIF_HOST=${APP_NAME}-ms-notif-${ENV_NAME}
MSNOTIF_NODE_VERSION=${NODE_VERSION}
MSNOTIF_FOLDER=notif
MSNOTIF_DB_NAME=msnotif
MSNOTIF_DB_USER=mbngh
MSNOTIF_DB_PWD=rgbndfçfhsdjkldsd787!!??
MSNOTIF_DB_MAX=50
MSNOTIF_DB_VERSION=1

# ms_cron
MSCRON_MIGRATION_HOST=${APP_NAME}-liquibase-ms-cron-${ENV_NAME}
MSCRON_HOST=${APP_NAME}-ms-cron-${ENV_NAME}
MSCRON_NODE_VERSION=${NODE_VERSION}
MSCRON_FOLDER=cron
MSCRON_DB_NAME=mscron
MSCRON_DB_USER=mbfgh
MSCRON_DB_PWD=rghdfçdhqsjklsqhdkl6567!!?
MSCRON_DB_MAX=50
MSCRON_DB_VERSION=1

# ovh smtp
SMTP_HOST=ssl0.ovh.net
SMTP_PORT=465
SMTP_USER=${SYSTEM_EMAIL}
SMTP_PASS=22dfrTgh66?!s3pg!G

# ms_mail
MSMAIL_MIGRATION_HOST=${APP_NAME}-liquibase-ms-mail-${ENV_NAME}
MSMAIL_HOST=${APP_NAME}-ms-mail-${ENV_NAME}
MSMAIL_URL=${SERVER_SCHEME}${MSMAIL_HOST}:${DEFAULT_PORT}
MSMAIL_NODE_VERSION=${NODE_VERSION}
MSMAIL_FOLDER=mail
MSMAIL_DB_NAME=msmail
MSMAIL_DB_USER=masdf
MSMAIL_DB_PWD=rdnsjkflqhflkdqfhl88??!
MSMAIL_DB_MAX=50
MSMAIL_DB_VERSION=1
MSMAIL_AMQP_QUEUE=nodemailer-amqp
MSMAIL_SMTP_HOST=${SMTP_HOST}
MSMAIL_SMTP_PORT=${SMTP_PORT}
MSMAIL_SMTP_FROM="Sender <${SYSTEM_EMAIL}>"
MSMAIL_SMTP_USER=${SMTP_USER}
MSMAIL_SMTP_PASS=${SMTP_PASS}


#excel
MSEXCEL_HOST=${APP_NAME}-ms-excel-${ENV_NAME}
MSEXCEL_NODE_VERSION=${NODE_VERSION}
MSEXCEL_FOLDER=excel
MSEXCEL_TEMPLATE_FOLDER=templates
MSEXCEL_PROCESS_DIR="/usr/src/python"
MSEXCEL_AMQP_QUEUE=excel-amqp

# ms_event
MSEVENT_MIGRATION_HOST=${APP_NAME}-liquibase-ms-event-${ENV_NAME}
MSEVENT_HOST=${APP_NAME}-ms-event-${ENV_NAME}
MSEVENT_URL=${SERVER_SCHEME}${MSEVENT_HOST}:${DEFAULT_PORT}
MSEVENT_NODE_VERSION=${NODE_VERSION}
MSEVENT_FOLDER=event
MSEVENT_DB_NAME=msevent
MSEVENT_DB_USER=reftf
MSEVENT_DB_PWD=fkgdqsjksdqlhdkls67567!??
MSEVENT_DB_MAX=50
MSEVENT_DB_VERSION=1

# ms_city
MSCITY_MIGRATION_HOST=${APP_NAME}-liquibase-ms-city-${ENV_NAME}
MSCITY_HOST=${APP_NAME}-ms-city-${ENV_NAME}
MSCITY_NODE_VERSION=${NODE_VERSION}
MSCITY_FOLDER=city
MSCITY_DB_NAME=mscity
MSCITY_DB_USER=reddf
MSCITY_DB_PWD=fkgufjdsjqklhl§§7798??
MSCITY_DB_MAX=50
MSCITY_DB_VERSION=1

# ms_org
MSORG_MIGRATION_HOST=${APP_NAME}-liquibase-ms-org-${ENV_NAME}
MSORG_HOST=${APP_NAME}-ms-org-${ENV_NAME}
MSORG_NODE_VERSION=${NODE_VERSION}
MSORG_FOLDER=org
MSORG_DB_NAME=msorg
MSORG_DB_USER=reddf
MSORG_DB_PWD=fcjqlkfnqdskjmldhjfl675!!?
MSORG_DB_MAX=50
MSORG_DB_VERSION=1


# ms_scraper
MSSCRAPER_NODE_VERSION=22.1.0-bookworm-slim
MSSCRAPER_HOST=${APP_NAME}-ms-scraper-${ENV_NAME}
MSSCRAPER_FOLDER=scraper
MSSCRAPER_TLS_METRO_URL=https://data.toulouse-metropole.fr/api/explore/v2.1/catalog/datasets/agenda-des-manifestations-culturelles-so-toulouse/records
MSSCRAPER_TLS_LIMIT=100

# grafana
GRAFANA_VERSION=grafana/grafana:10.2.2
GRAFANA_HOST=${APP_NAME}-grafana-${ENV_NAME}
GF_AUTH_ANONYMOUS_ENABLED=false
GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
GF_SERVER_HTTP_PORT=3300
GF_PATHS_PLUGINS=/grafana-plugins
GF_PATHS_PROVISIONING=/etc/grafana/provisioning/
GF_FEATURE_TOGGLES_ENABLE=newNavigation
GF_LOG_LEVEL=debug

# prometheus
PROMETHEUS_VERSION=prom/prometheus:v2.48.0
PROMETHEUS_HOST=${APP_NAME}-prometheus-${ENV_NAME}
PROMETHEUS_PORT=9090

# cadvisor
CADVISOR_VERSION=gcr.io/cadvisor/cadvisor:v0.47.2
CADVISOR_HOST=${APP_NAME}-cadvisor-${ENV_NAME}

# postgres-exporter
PG_EXPORTER_VERSION=quay.io/prometheuscommunity/postgres-exporter:v0.15.0
PG_EXPORTER_PORT=9187
PG_EXPORTER_HOST=${APP_NAME}-pg-exporter-${ENV_NAME}
PG_EXPORTER_SOURCE=postgresql://root:${POSTGRES_ROOT_USER}@${POSTGRES_HOST}:${POSTGRES_PORT}/postgres?sslmode=disable