/** Shared FPS cap for login WebGL canvases (background + frog). */
export const LOGIN_SHADER_FPS_CAP = 14;

/** Maximum frame delta in seconds to prevent lag spikes on tab switch. */
export const LOGIN_SHADER_DELTA_CAP = 0.1;

