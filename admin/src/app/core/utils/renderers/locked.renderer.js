export function buildLockedCellRenderer() {
    return (cellValue) => cellValue
        ? `<i class="pi pi-lock"></i>`
        : `<i class="pi pi-lock-open"></i>`;
}
//# sourceMappingURL=locked.renderer.js.map