export function buildActiveCellRenderer() {
    return (cellValue) => cellValue
        ? `<i class="pi pi-check-circle"></i>`
        : `<i class="pi pi-times-circle"></i>`;
}
//# sourceMappingURL=active.renderer.js.map