import { Environment } from "./environment.model";

export const environment: Environment = {
  production: false,
  gatelinApi: "/api/gatelin/",
  apiUsers: "/api/users/",
  assets: "assets",
  msNotifEnabled: true,
};
