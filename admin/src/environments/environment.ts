import { Environment } from "./environment.model";

export const environment: Environment = {
  production: false,
  apiGateway: "/api/gateway/",
  apiUsers: "/api/users/",
  assets: "assets",
  msNotifEnabled: true,
};
