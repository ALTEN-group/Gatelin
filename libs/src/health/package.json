{
  "name": "health",
  "version": "1.0.0",
  "description": "",
  "main": "route.js",
  "exports": {
    ".": "./route.js",
    "./test": "./test.js"
  },
  "scripts": {
    "dev": "nodemon -e js -x \"npm run build\"",
    "dev-poll": "nodemon -e js -x \"npm run build\" -L",
    "prebuild": "npm run test",
    "build": "npm pack --pack-destination ../../packs/health/",
    "postbuild": "cd ../../../app/ && npm i health --force --nofund --no-audit",
    "test": "echo \"Error: no test specified\""
  },
  "author": "lcluber",
  "license": "ISC",
  "dependencies": {
    "express": "4.20.0"
  },
  "devDependencies": {
    "nodemon": "2.0.22"
  }
}
